# The code below is an infinite loop.  The
# name describes exactly what it does, loop
# indefinitely.  This loop isn't useful
# in a real program, though.  Modify the
# code so the loop stops after the first
# iteration.

loop do
  puts 'Just keep pringint...'
  break
end